const Office = require('./office.model');
const Camera = require('./camera.model');
const Agent = require('./agent.model');
const Snapshot = require('./snapshot.model');
const ActivationCode = require('./activationCode.model');
const CameraAlert = require('./cameraAlert.model');
const AgentLog = require('./agentLog.model');

module.exports = {
  Office,
  Camera,
  Agent,
  Snapshot,
  ActivationCode,
  CameraAlert,
  AgentLog,
};